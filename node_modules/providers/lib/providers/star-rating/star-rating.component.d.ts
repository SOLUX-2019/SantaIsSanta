import { OnInit } from '@angular/core';
import { MatSnackBar } from '@angular/material/snack-bar';
export declare class StarRatingComponent implements OnInit {
    private snackBar;
    private rating;
    starCount: number;
    color: string;
    private ratingUpdated;
    private snackBarDuration;
    ratingArr: any[];
    constructor(snackBar: MatSnackBar);
    ngOnInit(): void;
    onClick(rating: number): boolean;
    showRatingLabel(level: number): string;
    showIcon(index: number): "star" | "star_border";
}
export declare enum StarRatingColor {
    primary = "primary",
    accent = "accent",
    warn = "warn"
}
