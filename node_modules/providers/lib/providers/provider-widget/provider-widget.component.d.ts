import { ChangeDetectorRef, EventEmitter, OnInit } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Provider } from '@brightpath/common';
import { ProvidersService } from '@brightpath/scheduling';
export declare class ProviderWidgetComponent implements OnInit {
    private providerService;
    private http;
    private cdr;
    provider: Provider;
    providerId: string;
    designatedProvider: boolean;
    onAppointmentCreated: EventEmitter<any>;
    onSelectedProvider: EventEmitter<any>;
    providerDetails: any;
    _showAvailability: boolean;
    availabilityLoaded: boolean;
    showRating: boolean;
    showDetails: boolean;
    showPrivateMessage: boolean;
    loaded: boolean;
    constructor(providerService: ProvidersService, http: HttpClient, cdr: ChangeDetectorRef);
    selectProvider(value: any): void;
    appointmentCreated(appointment: any): void;
    clearAvailability(): void;
    onAvailabilityLoaded(event: any): void;
    set showAvailability(availability: boolean);
    get showAvailability(): boolean;
    onRatingSaved(event: any): void;
    ngOnInit(): void;
}
