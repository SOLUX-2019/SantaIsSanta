import { EventEmitter, OnInit } from '@angular/core';
import { FormControl } from '@angular/forms';
import { HttpClient } from '@angular/common/http';
import { ActivatedRoute, Router } from '@angular/router';
import { MatSnackBar } from '@angular/material/snack-bar';
import { Provider } from '@brightpath/common';
import { ProvidersService } from '@brightpath/scheduling';
export declare class ProviderRatingWidgetComponent implements OnInit {
    private providerService;
    private http;
    private snackbar;
    private router;
    private route;
    provider: Provider;
    providerId: string;
    customerId: string;
    rating: number;
    ratings: any[];
    commentFormControl: FormControl;
    loading: boolean;
    loadingReviews: boolean;
    saved: EventEmitter<any>;
    constructor(providerService: ProvidersService, http: HttpClient, snackbar: MatSnackBar, router: Router, route: ActivatedRoute);
    ngOnInit(): void;
    onRatingChanged(rating: any): void;
    submitRating(): void;
}
