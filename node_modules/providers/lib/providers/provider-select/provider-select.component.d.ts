import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { StreamClientService } from '@brightpath/activity-stream';
import { CustomerService, ProvidersService } from '@brightpath/scheduling';
export declare class ProviderSelectComponent implements OnInit {
    private providerService;
    private customerService;
    private streamClient;
    private router;
    customerId: any;
    customer: any;
    loading: boolean;
    providers: any[];
    constructor(providerService: ProvidersService, customerService: CustomerService, streamClient: StreamClientService, router: Router);
    ngOnInit(): void;
    loadProviders(): void;
    onProviderSelected(newProvider: any): void;
}
