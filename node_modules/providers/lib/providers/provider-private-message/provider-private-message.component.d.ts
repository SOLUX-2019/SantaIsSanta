import { EventEmitter, OnInit } from '@angular/core';
import { MatSnackBar } from '@angular/material/snack-bar';
import { StreamActivity, StreamClientService } from '@brightpath/activity-stream';
export declare class ProviderPrivateMessageComponent implements OnInit {
    private streamClientService;
    private snackbar;
    customerId: any;
    providerId: any;
    close: EventEmitter<boolean>;
    constructor(streamClientService: StreamClientService, snackbar: MatSnackBar);
    onPrivateMessage(privateMessage: StreamActivity): void;
    ngOnInit(): void;
}
